Update and inspect the repository knowledge graph.

Run `graphify update .`. Then use `graphify query` for the requested concept or relationship. Report the resulting graph counts and any relevant architectural observations.

Query: $ARGUMENTS